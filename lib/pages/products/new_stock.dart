import 'package:flutter/material.dart';

class StockEntryForm extends StatelessWidget {
  const StockEntryForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Stock Entry'),),
      body: Center(child: Text('Stock Entry'),),
    );
  }
}
